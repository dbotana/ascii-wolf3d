# dist/wolf3d.html is generated by `node reference/bundle.js` — it is committed
# so the game can be deployed and handed around as one self-contained file, but
# it is not the thing you review. Mark it generated so it collapses in diffs and
# does not skew language stats.
dist/wolf3d.html -diff linguist-generated
